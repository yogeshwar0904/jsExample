/*
Stack is a size storage  and memory allocation.
Stores the primitive and symboles.
Every data have separate entities and so they Immutable.
Size and memory Allocation Fixed at Compile time.
Stack also know as Static memory allocation Last In First Out.
*/

let firstName = "yoga";

let lastName = firstName;
firstName = "San";

console.log(firstName);
console.log(lastName);